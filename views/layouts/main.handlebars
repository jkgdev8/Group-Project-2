<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Subscription Calendar</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <div class="wrapper">
    <header>
      <h1>
        {{#if loggedIn}}
        <a href="/dashboard">Subscription Calendar</a>
        {{else}}
        <a href="/">Subscription Calendar</a>
        {{/if}}
      </h1>
      <nav>
        {{!-- log out BTN --}}
        {{#if loggedIn}}

        {{!-- <button class="btn-no-style">profile</button> --}}
        <button id="logout" class="btn-no-style">LOGOUT</button>
        {{/if}}
      </nav>
    </header>
    <main>
      {{{ body }}}
    </main> 
  </div>
  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>